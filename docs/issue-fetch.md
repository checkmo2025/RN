# Fetch·액션 점검 이슈 정리 (푸시알림 제외)

- 작성일: 2026-04-27
- 범위: 관리자 API 제외, 앱에서 실제 사용하는 조회/액션 fetch 흐름 (푸시알림 제외)
- 기준: Swagger(`https://api.checkmo.co.kr/swagger-ui/index.html#/`) + OpenAPI + RN 코드 + 일부 실서버 응답 샘플

## 판정 기준

- `RN`: 프론트 구현/UX/에러처리/페이징 반영 문제
- `BE`: 서버 동작/응답 형식/권한 정책 문제
- `BE 문서`: 서버는 동작하지만 Swagger 문서와 실제가 불일치
- `공동`: 양쪽 합의 후 함께 정리해야 하는 항목

## 회원 영역

- `(완료) [MEM-01] 내 신고내역 날짜 필드 불일치`
- 판정: `RN`
- 내용: `/members/me/reports` 스키마는 `reportDate` 중심인데 앱은 `createdAt` 기준으로 표시함.
- 방향: 앱 매핑에서 `reportDate` 우선 처리, 하위 호환으로 `createdAt` 보조 처리.
- 현황: 2026-04-27 RN 반영 완료 (`reportDate ?? createdAt` 매핑 적용)

- `(완료) [MEM-02] 내 신고내역 페이징 미반영`
- 판정: `RN`
- 내용: API는 `hasNext/nextCursor`를 제공하지만 화면에서는 단건 조회 성격으로 사용 중.
- 방향: 커서 루프를 공통화해서 신고내역도 끝 페이지까지 수집.
- 현황: 2026-04-27 RN 반영 완료 (`/members/me/reports` 커서 기반 전체 수집 적용)

- `(완료) [MEM-03] 일반 회원 플로우에 관리자 API fallback 잔존`
- 판정: `RN`
- 내용: `/members/me/reports` 실패 시 `/admin/.../reports` 호출 시도 로직이 남아 있음.
- 방향: 일반 사용자 경로에서 관리자 fallback 제거, 실패 시 사용자 메시지/재시도 정책으로 통일.
- 현황: 2026-04-27 RN 반영 완료 (관리자 fallback 제거)

- `(RN 정리 완료) [MEM-04] find-email GET fallback`
- 판정: `공동` (우선 `BE 문서` + `RN 정리`)
- 내용: 문서는 POST만 정의되어 있는데 앱에 GET fallback이 남아 있음.
- 방향: 서버 정책을 하나로 확정(POST only 권장)하고 문서/서버/앱 fallback 정리.
- 현황: 2026-04-28 RN 반영 완료 (GET fallback 제거, POST 단일 경로로 정리)

- `(완료) [MEM-05] 닉네임 사전검증 규칙 약함`
- 판정: `RN`
- 내용: 길이 검증 위주라 서버 패턴 오류를 클라이언트에서 미리 걸러내지 못함.
- 방향: 서버 패턴/길이 규칙을 클라이언트 동일 규칙으로 선검증.
- 현황: 2026-04-27 RN 반영 완료 (닉네임 정규식 선검증 추가)

## 책이야기 영역

- `(완료) [BOOK-01] 수정 화면 UX와 서버 수정 가능 범위 불일치`
- 판정: `RN`
- 내용: 수정 화면에서 제목/책 변경이 가능한 것처럼 보이지만 실제 update 요청은 `description`만 반영.
- 방향: 수정 모드에서 제목/책 선택 비활성화 또는 "본문만 수정 가능" 명시.
- 현황: 2026-04-27 RN 반영 완료 (수정 모드에서 책/제목 변경 비활성화 + 본문만 수정 안내 추가)

- `(완료) [BOOK-02] 일부 화면에서 책이야기 페이징 미반영`
- 판정: `RN`
- 내용: 내 프로필/다른 사람 프로필은 커서 반복 없이 첫 페이지만 사용.
- 방향: Story 메인과 동일한 커서 루프 적용, 공통 fetch 유틸 재사용.
- 현황: 2026-04-27 RN 반영 완료 (마이페이지/다른 사람 프로필 책이야기 커서 기반 전체 수집 적용)

- `(완료) [BOOK-03] 피드 조회 실패가 무음 처리되는 구간 존재`
- 판정: `RN`
- 내용: 일부 ApiError가 사용자에게 안내되지 않아 실패 원인 파악이 어려움.
- 방향: 조회 실패 UX 정책을 통일(401/403/404/5xx별 메시지 최소 기준 정의).
- 현황: 2026-04-27 RN 반영 완료 (책이야기 피드/프로필 조회 ApiError 메시지 가시화)

- `[BOOK-04] 공개/인증 필요 여부 문서와 실제 차이`
- 판정: `BE 문서`
- 내용: 일부 엔드포인트(`all/member/search`)는 익명 200인데 문서는 401 응답이 함께 기술되어 혼동 가능.
- 방향: 실제 권한 정책 기준으로 Swagger 응답/설명 업데이트.

- `[BOOK-05] 날짜 형식 표기 차이`
- 판정: `BE 문서`
- 내용: 스키마는 `date-time`인데 실제는 `YYYY-MM-DD HH:mm` 형태 응답이 존재.
- 방향: RFC3339로 통일하거나, 현재 포맷을 문서에 명확히 명시.

## 소식 영역

- `(완료) [NEWS-01] fetchNewsList에서 페이징 메타 유실`
- 판정: `RN`
- 내용: 서비스 함수가 목록 배열만 반환해서 `hasNext/nextCursor` 정보를 상위 화면이 못 씀.
- 방향: `fetchNewsList` 반환 타입을 목록+커서 메타 구조로 정리.
- 현황: 2026-04-27 RN 반영 완료 (`fetchNewsList` 반환을 목록+커서 메타 구조로 정리)

- `(완료) [NEWS-02] News/Home 목록이 구조적으로 1페이지만 사용`
- 판정: `RN`
- 내용: 서비스 반환 타입 한계로 추가 페이지를 가져오지 못함.
- 방향: `NewsScreen`, `HomeScreen` 모두 커서 기반 누적 로딩 지원.
- 현황: 2026-04-27 RN 반영 완료 (`NewsScreen`, `HomeScreen` 커서 기반 누적 로딩 적용)

- `(완료) [NEWS-03] 소식 상세/목록 ApiError 무음 처리`
- 판정: `RN`
- 내용: `ApiError`는 토스트 없이 빠지는 구간이 있어 실패 체감이 약함.
- 방향: 상세 진입 실패 시 사용자 안내 문구/복귀 동작 표준화.
- 현황: 2026-04-27 RN 반영 완료 (소식 목록/상세 ApiError 메시지 가시화)

- `[NEWS-04] 소식 조회 권한 문서-실서버 불일치`
- 판정: `BE 문서`
- 내용: `/news`, `/news/{id}`는 익명 조회 가능(실서버)인데 문서엔 401 케이스가 혼재.
- 방향: 공개 API 여부를 문서에 명확히 고정하고 응답 케이스 정리.

- `(완료) [NEWS-05] 미사용 fetch 함수 잔존`
- 판정: `RN`
- 내용: `fetchNewsCarousel`은 현재 호출처 없음.
- 방향: 제거 또는 실제 사용처 연결 중 하나로 정리.
- 현황: 2026-04-27 RN 반영 완료 (미사용 `fetchNewsCarousel` 제거)

## 책 검색 영역

- `(완료) [BOOKS-01] 검색 결과 페이징 미반영`
- 판정: `RN`
- 내용: `searchBooks(keyword, 1)` 고정 호출로 `hasNext/currentPage`를 화면에서 사용하지 않음.
- 방향: 검색 결과 목록에 페이지 상태를 유지하고 다음 페이지 누적 로딩(무한 스크롤/더보기)으로 확장.
- 현황: 2026-04-27 RN 반영 완료 (책 검색 결과에 `hasNext/currentPage` 상태 + 더보기 누적 로딩 적용)

- `(완료) [BOOKS-02] 검색/추천 좋아요 상태 소스 불일치`
- 판정: `RN`
- 내용: API 응답의 `likedByMe`를 화면 상태로 쓰지 않고, 인메모리 `bookLikeApi`만 사용해 재진입/재실행 시 상태 일관성이 깨질 수 있음.
- 방향: `BookItem`에 서버 좋아요 상태를 반영하고 `/books/me/likes`와 단일 소스 기준으로 동기화.
- 현황: 2026-04-27 RN 반영 완료 (`BookItem.likedByMe` 반영 + `/books/me/likes` 기준 동기화, `bookLikeApi` 의존 제거)

- `[BOOKS-03] 다른 회원 좋아요 목록 권한 문서-실응답 불일치`
- 판정: `BE 문서`
- 내용: Swagger에선 `/books/{memberNickname}/likes`에 401이 명시되지만, 실서버는 비로그인으로도 기존 회원 조회 시 200 응답.
- 방향: 실제 정책이 공개 조회라면 401 문서 제거, 비공개 정책이면 서버 권한 체크를 강화.

- `[BOOKS-04] nullable 필드 문서 누락`
- 판정: `BE 문서`
- 내용: 실응답에서 `currentPage`(recommend/빈 검색), `nextCursor`(좋아요 목록 마지막 페이지)가 `null`인데 스키마는 정수로만 정의됨.
- 방향: Swagger에 `nullable`을 명시하거나 서버 응답을 항상 숫자 값으로 고정.

- `[BOOKS-05] 검색/추천 "결과 없음" 상태코드 문서 불일치`
- 판정: `BE 문서`
- 내용: Swagger에는 404(`책 정보를 찾을 수 없음`)가 정의되어 있으나, 실서버는 결과 없음 케이스에서 200 + 빈 배열을 반환.
- 방향: 결과 없음 정책을 200(빈 목록)으로 문서 고정하거나, 404 전략이면 서버/앱 처리 기준을 함께 재정의.

- `(완료) [BOOKS-06] 좋아요 전체 조회 루프 하드 캡(20페이지)`
- 판정: `RN`
- 내용: `fetchAllMyLikedBooks`/`fetchAllMemberLikedBooks`가 최대 20회 루프로 제한되어 대량 데이터에서 누락 가능성이 있음.
- 방향: 커서 종료 조건 중심으로 재구성하고, 안전 캡 도달 시 사용자/로그 경고를 남기도록 보강.
- 현황: 2026-04-27 RN 반영 완료 (커서 순환 방지 + 루프 상한 확장)

## 모임 검색 영역

- `(완료) [MEET-SEARCH-01] 비로그인 기본 진입 시 추천 API(401) 호출`
- 판정: `RN`
- 내용: 모임 검색 기본 상태(검색어 없음/필터 없음)에서 `search`가 아니라 `recommendations`를 먼저 호출하는데, 실서버는 비로그인 401 응답.
- 방향: 비로그인 기본 로딩은 `search` 기반 목록으로 전환하고, 로그인 사용자에게만 추천 API를 사용.
- 현황: 2026-04-27 RN 반영 완료 (비로그인 기본 진입에서 추천 API 대신 `search` 경로 사용)

- `[MEET-SEARCH-02] 추천 API 권한 문서-실서버 불일치`
- 판정: `BE 문서`
- 내용: Swagger `/clubs/recommendations`는 200만 정의되어 있으나, 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: 인증 필요 정책을 Swagger에 명시(401 추가)하거나, 비회원 허용 정책으로 서버 동작을 조정.

- `(완료) [MEET-SEARCH-03] 모임 검색 페이징 미반영`
- 판정: `RN`
- 내용: `/clubs/search`는 `hasNext/nextCursor`를 제공하지만 화면은 첫 요청만 사용하고 다음 페이지를 조회하지 않음.
- 방향: 커서 기반 누적 로딩(무한 스크롤/더보기)을 도입하고 `nextCursor`를 상태로 관리.
- 현황: 2026-04-27 RN 반영 완료 (`/clubs/search` 커서 기반 누적 로딩 + 중복 제거 적용)

- `(완료) [MEET-SEARCH-04] 검색 실패가 무음 처리되는 구간 존재`
- 판정: `RN`
- 내용: 모임 검색/추천 fetch에서 `ApiError`는 토스트 없이 바로 return되어, 400/401/500이 사용자에게 단순 "결과 없음"으로 보일 수 있음.
- 방향: 상태코드별 최소 안내 UX를 정의(예: 401 로그인 유도, 400 입력 수정 안내, 5xx 재시도 안내).
- 현황: 2026-04-27 RN 반영 완료 (검색/추천 조회 오류 메시지 가시화)

- `[MEET-SEARCH-05] 키워드 길이 초과 시 500 응답`
- 판정: `BE`
- 내용: Swagger는 `keyword.maxLength=40`로 안내하지만, 40자 초과 요청 시 400 검증 오류 대신 500(`COMMON_500`)이 내려옴.
- 방향: 서버 입력 검증 예외를 400으로 정규화하고, 에러 응답을 일관된 검증 메시지 포맷으로 반환.

- `(완료) [MEET-SEARCH-06] 키워드 길이 클라이언트 사전검증 부재`
- 판정: `RN`
- 내용: 검색 입력값에 40자 제한이 없어 서버 오류 케이스를 앱에서 그대로 유발 가능.
- 방향: 검색어 길이 제한/입력 가이드를 UI에서 선적용해 비정상 요청을 사전에 차단.
- 현황: 2026-04-27 RN 반영 완료 (검색 입력 `maxLength=40` + 로더 선검증 추가)

## 마이페이지 영역

- `[MYPAGE-01] 내 모임 API 권한 응답 문서 누락`
- 판정: `BE 문서`
- 내용: `/me/clubs` Swagger는 200/400만 명시하지만, 실서버 비로그인 호출은 401(`UNAUTHORIZED`)를 반환.
- 방향: Swagger에 401 응답을 추가해 실제 인증 정책과 문서를 일치.

- `(완료) [MYPAGE-02] 내 알림/내 소식 조회 실패 무음 처리`
- 판정: `RN`
- 내용: `MyPageScreen`의 `loadAllNotifications`/`loadMyNews`는 `ApiError`를 대부분 토스트 없이 비우기 처리해, 서버 오류가 "데이터 없음"처럼 보일 수 있음.
- 방향: 상태코드별 최소 피드백 정책 적용(401 로그인 유도, 5xx 재시도 안내), 빈 상태 UI와 오류 상태 UI 분리.
- 현황: 2026-04-27 RN 반영 완료 (`ApiError` 상태코드 기반 토스트 노출 적용)

- `(완료) [MYPAGE-03] 내 알림/내 소식 전체 수집 루프 하드 캡(20페이지)`
- 판정: `RN`
- 내용: `loadAllNotifications`/`loadMyNews`가 최대 20회 루프로 고정되어 대량 데이터에서 누락 가능성이 있음.
- 방향: 커서 종료 조건 중심으로 재구성하고, 안전 캡 도달 시 로그/사용자 안내를 남기도록 보강.
- 현황: 2026-04-27 RN 반영 완료 (커서 순환 방지 + 루프 상한 확장 + 중복 제거 적용)

- `(완료) [MYPAGE-04] 비로그인 마이페이지에 더미 데이터 노출`
- 판정: `RN`
- 내용: 비로그인 시 `fallbackStories`/`fallbackGroups`를 실제 데이터처럼 렌더링해 현재 로그인 상태와 정보 신뢰도가 혼동될 수 있음.
- 방향: 비로그인 상태는 명시적 Empty + 로그인 유도 CTA로 전환하고 더미 데이터는 개발/프리뷰 모드에서만 사용.
- 현황: 2026-04-27 RN 반영 완료 (더미 데이터 제거 + 비로그인 안내/로그인 유도 CTA 적용)

## 모임 내부 - 모임 홈화면 영역

- `(완료) [MEET-HOME-01] 비로그인 홈 진입 시 멤버십 API 강제 호출`
- 판정: `RN`
- 내용: 홈화면 워크스페이스 로더가 비로그인에서도 `/clubs/{clubId}/me`를 포함한 요청을 수행하고, 401 발생 시 `handleAuthExpired -> requireAuth`로 즉시 로그인 유도됨.
- 방향: 비로그인일 때는 공개 API(`/clubs/{clubId}/home`, `/clubs/{clubId}/notices/latest`)만 호출하고 멤버십/운영진 API는 로그인 후 조건부 실행.
- 현황: 2026-04-27 RN 반영 완료 (`fetchClubMyMembership` 호출을 `isLoggedIn` 조건부로 변경)

- `[MEET-HOME-02] 내 클럽 상태 조회 API 권한 문서 누락`
- 판정: `BE 문서`
- 내용: Swagger `/clubs/{clubId}/me`는 200/404만 정의하지만, 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: Swagger에 401 응답을 명시해 권한 정책과 문서 일치.

- `[MEET-HOME-03] 이번 모임 바로가기 권한/응답 문서-실서버 불일치`
- 판정: `공동` (`RN` + `BE 문서`)
- 내용: `/clubs/{clubId}/meetings/next` 문서는 302/403/404 중심인데 비로그인 실서버는 401 응답. 버튼 핸들러도 사전 인증 체크 없이 호출.
- 방향: 문서에 401 추가 + 클라이언트에서 비로그인 사전 차단(`requireAuth`) 또는 401 전용 UX로 정리.

- `(완료) [MEET-HOME-04] 홈 진입 시 중복 홈 데이터 조회`
- 판정: `RN`
- 내용: 모임 선택 직후 `openGroupHome`에서 `fetchClubHome` 1회 호출 후, `GroupHomeView`의 워크스페이스 로딩에서 `fetchClubHome`을 다시 호출.
- 방향: 첫 진입 시 전달된 홈 데이터를 재사용하고, 화면 내부는 갱신 목적(당겨서 새로고침/포커스 복귀)으로만 재조회하도록 단일화.
- 현황: 2026-04-28 RN 반영 완료 (`openGroupHome` 선행 조회 제거, 워크스페이스 로더 단일화)

## 모임 내부 - 공지사항 영역

- `[MEET-NOTICE-01] 공지 조회 API 권한 응답 문서 누락(401)`
- 판정: `BE 문서`
- 내용: `/clubs/{clubId}/notices*` Swagger는 200/403/404 중심인데, 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: 공지 목록/상세/댓글/투표 API에 401 응답을 문서에 명시해 실제 권한 정책과 일치.

- `(완료) [MEET-NOTICE-02] 공지 목록 서버 페이징 미반영`
- 판정: `RN`
- 내용: 앱은 `fetchClubNotices(clubId, 1)`만 호출한 뒤 로컬 `noticePage`로만 나눠 보여주며, 서버 `page/hasNext/totalPages`를 사용하지 않음.
- 방향: 서버 페이지네이션 기준으로 다음 페이지를 추가 로딩하고, 로컬 페이지 분할은 제거하거나 보조로만 사용.
- 현황: 2026-04-27 RN 반영 완료 (워크스페이스 로드 시 `hasNext` 루프로 모든 공지 서버 페이지 수집)

- `(완료) [MEET-NOTICE-03] 공지 API 실패 무음 처리 구간 존재`
- 판정: `RN`
- 내용: 공지 상세/댓글/투표/생성·수정·삭제 처리에서 `ApiError`를 다수 구간에서 토스트 없이 무시해 실패 원인을 사용자에게 전달하지 못함.
- 방향: 상태코드별 최소 피드백 정책 적용(401 로그인 유도, 403 권한 안내, 5xx 재시도 안내).
- 현황: 2026-04-27 RN 반영 완료 (워크스페이스/공지상세/댓글 로드 실패 시 403·기타 ApiError 토스트 노출)

- `(완료) [MEET-NOTICE-04] 비멤버(403) 안내 UX 부재`
- 판정: `RN` (추론)
- 내용: 공지 API 스펙상 비멤버는 403 가능하지만, 현재 로직은 공지 탭에서 권한 오류를 별도 안내하지 않아 빈 화면처럼 보일 가능성이 있음.
- 방향: 403 전용 빈상태 메시지/가입 유도 CTA를 추가하고, 공지 접근 조건(멤버 전용 여부)을 화면에 명시.
- 현황: 2026-04-27 RN 반영 완료 (MEET-NOTICE-03 동일 대응 — 403 시 "모임 멤버만 열람할 수 있습니다." / "공지 열람 권한이 없습니다." 토스트 표시)

## 모임 내부 - 책장 영역 (운영진/멤버 기능 포함)

- `[MEET-BOOKSHELF-01] 책장/정기모임 API 권한 응답 문서 누락(401)`
- 판정: `BE 문서`
- 내용: Swagger `/clubs/{clubId}/bookshelves*`, `/clubs/{clubId}/meetings*`는 200/403/404 중심인데, 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답을 반환.
- 방향: 책장 목록/상세/발제/한줄평/수정, 정기모임 조회/팀발제/팀채팅/조관리/이번모임 바로가기 API에 401 응답을 문서에 명시.

- `(완료) [MEET-BOOKSHELF-02] 책장 목록 서버 페이징 미반영`
- 판정: `RN`
- 내용: `fetchClubBookshelves`는 `hasNext/nextCursor`를 제공하지만, 화면은 첫 호출 결과만 사용(초기 로드/생성 후/삭제 후 모두 동일)해 누락 가능성이 있음.
- 방향: 커서 기반 누적 로딩(전체 수집 또는 더보기)을 적용해 다건 책장 누락을 방지.
- 현황: 2026-04-27 RN 반영 완료 (초기/생성/삭제 후 모두 `/bookshelves` 커서 누적 수집 적용)

- `(완료) [MEET-BOOKSHELF-03] 리뷰/팀발제/팀채팅 전체 수집 루프 하드 캡(20페이지)`
- 판정: `RN`
- 내용: `fetchAllBookshelfReviewsForMeeting`, `fetchAllMeetingTeamTopics`, `fetchAllMeetingTeamChats`가 `for (page < 20)` 고정 루프라 대량 데이터에서 누락될 수 있음.
- 방향: 종료 조건을 `hasNext/nextCursor` 중심으로 재구성하고, 안전 캡 도달 시 로그/사용자 안내를 남기도록 보강.
- 현황: 2026-04-27 RN 반영 완료 (책장/팀발제/팀채팅 루프 상한 확장 + 커서 순환 방지 유지)

- `(완료) [MEET-BOOKSHELF-04] 책장 ID와 정기모임 ID 혼용 리스크`
- 판정: `RN` (추론)
- 내용: 같은 파일에 `detail.meetingId = 정기모임 ID`, `meetingId = 책장 ID` 주석이 있는데, 조관리 저장/정기모임 조회/STOMP 채팅 연결은 `selectedBookshelfBook.remoteMeetingId`를 `/meetings/*` 경로에 직접 사용.
- 방향: 상태를 `bookshelfMeetingId`와 `regularMeetingId`로 분리 저장하고, `/meetings/*`·채팅 구독에는 정기모임 ID만 사용하도록 정규화.
- 현황: 2026-04-28 RN 반영 완료 (`regularMeetingId` 분리 저장, meetings/STOMP/조관리 호출 경로 정규화)

- `(완료) [MEET-BOOKSHELF-05] 운영진/작성 액션 ApiError 무음 처리`
- 판정: `RN`
- 내용: 발제·한줄평 작성/수정, 조관리 저장, 책장 생성·수정·삭제에서 `ApiError`를 토스트 없이 무시하는 구간이 있어 실패 원인 전달이 약함.
- 방향: 상태코드별 최소 피드백 정책 적용(401 로그인 유도, 403 권한 안내, 404 대상 없음, 5xx 재시도 안내).
- 현황: 2026-04-27 RN 반영 완료 (책장 운영/작성 액션 공통 에러 메시지 가시화 적용)

## 인증/온보딩 영역

- `(RN 보완 완료) [AUTH-01] 회원가입 완료 플로우 비원자성`
- 판정: `공동`
- 내용: `회원가입 -> 로그인 -> 프로필 이미지 업로드 -> 추가정보 저장`이 단계별 호출로 분리되어, 중간 실패 시 계정만 생성되고 온보딩이 미완료된 상태가 남을 수 있음.
- 방향: 서버 트랜잭션/단계별 상태모델(온보딩 단계) 도입 또는 클라이언트 보상 로직(재시도/롤백 안내)으로 정규화.
- 현황: 2026-04-28 RN 보완 완료 (409 재진입 처리 + 단계별 실패 안내/재시도 UX 추가, 완전 원자성은 BE 협의 필요)

- `[AUTH-02] 로그인 상태 조회 API 권한 응답 문서 누락`
- 판정: `BE 문서`
- 내용: Swagger `/members/me/login-status`는 200만 정의되어 있으나, 실서버 비로그인 호출은 401(`UNAUTHORIZED`)를 반환.
- 방향: Swagger에 401 응답을 추가하고 인증 필요 엔드포인트로 명시.

- `(완료) [AUTH-03] 추가정보 입력 스키마 대비 클라이언트 선검증 일부 누락`
- 판정: `RN`
- 내용: Swagger `AdditionalInfo`는 `name.maxLength=10`, `nickname.pattern` 제약이 있으나, 앱은 `name` 길이/닉네임 패턴을 선검증하지 않아 서버 검증 오류를 사용자가 늦게 확인함.
- 방향: 회원가입 추가정보 단계에서 서버 스키마 기반 사전검증을 동일하게 적용.
- 현황: 2026-04-27 RN 반영 완료 (`name.trim().length > 10` 선검증 추가, `nickname.pattern`은 기존 `nicknameRegex`로 이미 적용 중)

## 회원 액션 영역 (프로필/팔로우/설정)

- `(완료) [MEM-06] 팔로워/팔로잉 전체 수집 루프 하드 캡(20페이지)`
- 판정: `RN`
- 내용: `MyPageScreen`, `UserProfileScreen`의 `fetchAllFollowUsers`가 `for (i < 20)` 고정 루프로 구현되어 대량 데이터 누락 가능성이 있음.
- 방향: `hasNext/nextCursor` 종료 조건 중심으로 재구성하고, 안전 캡 도달 시 로그/사용자 안내를 추가.
- 현황: 2026-04-27 RN 반영 완료 (커서 순환 방지 + 루프 상한 확장)

- `[MEM-07] 내 계정 API 권한 응답 문서 누락(401)`
- 판정: `BE 문서`
- 내용: Swagger `/members/me/follow-count`, `/members/me/reports`, `/members/me/update-password`는 200 중심으로 정의되어 있으나, 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: 인증 필수 API로 401 응답 스펙을 Swagger에 명시.

- `(완료) [MEM-08] 비밀번호 변경 클라이언트 선검증 부족`
- 판정: `RN`
- 내용: 마이페이지 비밀번호 변경은 `일치 여부`만 확인하고, 서버 스키마(`6~12자`, 영문+특수문자)를 클라이언트에서 선검증하지 않음.
- 방향: 회원가입 비밀번호 규칙과 동일한 정규식/길이 검증을 비밀번호 변경 화면에도 적용.
- 현황: 2026-04-27 RN 반영 완료 (마이페이지 비밀번호 규칙 검증 추가)

- `(RN UX 정리 완료) [MEM-09] 사용자 차단 기능이 플레이스홀더 상태`
- 판정: `공동`
- 내용: 다른 사람 프로필의 차단 액션은 확인 Alert 후 `"차단했습니다.(백엔드 처리중)"` 토스트만 노출되고 실제 API 연동이 없음.
- 방향: 차단 API 계약(등록/해제/목록) 확정 후 앱 액션을 실제 호출로 연결하고, 실패/해제 UX를 포함해 완성.
- 현황: 2026-04-28 RN 반영 완료 (오해를 주는 성공형 플레이스홀더 제거, 준비중 안내 UX로 정리)

## 모임 운영 액션 영역 (생성/수정/운영관리)

- `(완료) [MEET-MGMT-01] 모임 생성/수정 입력 제한 선검증 부재`
- 판정: `RN`
- 내용: Swagger `ClubDetail` 제약(`name<=40`, `description<=500`, `region<=40`, `contact.label<=20`, `contact.link<=100`)이 UI에서 대부분 강제되지 않고 placeholder 안내에만 의존함.
- 방향: 입력 컴포넌트 `maxLength`와 제출 전 검증을 스키마와 동일하게 적용.
- 현황: 2026-04-27 RN 반영 완료 (생성/수정 폼 name·description·region·link label·link url에 `maxLength` 적용)

- `(완료) [MEET-MGMT-02] 생성 화면 링크 개수 초과 입력이 무음 절단됨`
- 판정: `RN`
- 내용: 생성 화면은 링크 입력을 4개 초과해 추가할 수 있지만, 제출 시 `.slice(0,4)`로 뒤 항목을 조용히 제외해 데이터 유실 UX가 발생함.
- 방향: UI 단계에서 4개 상한을 명시적으로 제한하거나, 초과 시 사용자 안내 후 정리.
- 현황: 2026-04-27 RN 반영 완료 (4개 초과 시 추가 버튼 → "링크는 최대 4개까지 추가할 수 있습니다." 안내 텍스트로 대체)

- `(완료) [MEET-MGMT-03] 모임 수정 화면 카테고리 최대 6개 제한 미적용`
- 판정: `RN`
- 내용: 생성 화면은 최대 6개 제한이 있으나, 수정 화면 카테고리 토글은 상한 없이 선택 가능해 서버 검증 오류를 유발할 수 있음.
- 방향: 수정 화면에도 생성과 동일한 선택 상한 로직을 적용.
- 현황: 2026-04-27 RN 반영 완료 (수정 화면 카테고리 토글에 `>= 6` 상한 추가)

- `[MEET-MGMT-04] 모임 수정 요청에서 링크 필드 미전송`
- 판정: `공동`
- 내용: `/clubs/{clubId}` 요청 스키마는 `links`를 허용하지만 현재 수정 저장 payload에는 링크가 포함되지 않아, 서버 처리 정책에 따라 링크 보존/유실 동작이 불명확함.
- 방향: 서버의 "미전송 필드 처리 정책"을 확정하고, 앱 수정 화면에서 링크 편집/전송 정책을 문서와 동일하게 정리.

- `[MEET-MGMT-05] 모임 운영/관리 API 권한 응답 문서 누락(401)`
- 판정: `BE 문서`
- 내용: Swagger `/clubs`(POST), `/clubs/{clubId}`(PUT/DELETE), `/clubs/{clubId}/members*`는 401이 누락된 케이스가 있으나 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: 운영/관리 API 전반에 401 응답을 명시하고 인증 요구조건 설명을 통일.

## 실시간 채팅 영역

- `(완료) [CHAT-01] STOMP 실시간 수신 메시지 중복 방지 미적용`
- 판정: `RN`
- 내용: 초기 히스토리 로딩은 `messageId` 중복 제거가 있으나, STOMP `onMessage` 수신분은 바로 append되어 재연결/중복 전송 시 중복 렌더링 가능성이 있음.
- 방향: 활성 채팅방별 `messageId` 기준 dedupe를 실시간 수신 경로에도 적용.
- 현황: 2026-04-28 RN 반영 완료 (실시간 수신 append 경로에 `messageId` dedupe 적용)

- `[CHAT-02] REST 채팅 전송 함수가 스펙/실사용과 불일치`
- 판정: `공동`
- 내용: `sendClubMeetingTeamChatMessage`는 `POST /chat/messages`를 호출하도록 남아 있으나 Swagger는 해당 경로 `GET`만 정의, 앱 실제 전송은 STOMP만 사용함.
- 방향: 서버 계약 기준으로 REST 전송 경로를 확정(유지/폐기)하고, 미사용 함수 제거 또는 정식 경로로 정리.

- `[CHAT-03] 채팅 히스토리 조회 API 권한 응답 문서 누락(401)`
- 판정: `BE 문서`
- 내용: Swagger `/clubs/{clubId}/meetings/{meetingId}/teams/{teamId}/chat/messages`는 200/400만 정의되어 있으나, 실서버 비로그인 호출은 401(`UNAUTHORIZED`) 응답.
- 방향: 채팅 히스토리 API 문서에 401 응답을 추가하고 인증 필수 정책을 명시.

## 신고 영역 (회원/책이야기/모임 공통)

- `[REPORT-01] 공지/댓글/발제 신고가 대상 엔티티를 식별하지 못함`
- 판정: `공동`
- 내용: 현재 신고 API payload는 `reportedMemberNickname/reportType/content`만 전달되어, "어떤 공지/댓글/발제를 신고했는지" 식별자가 서버에 전달되지 않음.
- 방향: 신고 도메인 모델에 `domainType/domainId/sourceId` 등 대상 식별자를 확장하고 앱 신고 호출을 엔티티 기반으로 전환.

- `(완료) [REPORT-02] 신고 모달 타입 선택이 문맥 비의존`
- 판정: `RN`
- 내용: 신고 모달이 모든 화면에서 동일 타입 목록(`GENERAL/BOOK_STORY/COMMENT/CLUB_MEETING`)을 노출해 문맥과 맞지 않는 유형 선택이 가능함.
- 방향: 진입 컨텍스트(책이야기, 공지댓글, 모임발제)에 맞는 타입만 노출하거나 기본 타입 고정 + 보조 사유만 입력받도록 단순화.
- 현황: 2026-04-28 RN 반영 완료 (`allowedTypes` 기반으로 화면 진입 컨텍스트별 신고 타입 제한)

- `(완료) [REPORT-03] 신고 본문 최대 길이 클라이언트-서버 불일치`
- 판정: `RN`
- 내용: 서버 스키마는 `content <= 500`인데 앱 모달 입력은 `maxLength=400`으로 제한해 사용 가능한 입력 범위를 축소함.
- 방향: 모달 최대 길이를 서버 스펙(500)과 일치시키고 카운터 문구도 함께 정리.
- 현황: 2026-04-27 RN 반영 완료 (`maxLength` 400 → 500, placeholder 문구 일치)

## 진행 현황 (2026-04-28 기준)

### RN 완료 (업데이트)
`MEM-01`, `MEM-02`, `MEM-03`, `MEM-05`, `MEM-06`, `MEM-08`,
`BOOK-01`, `BOOK-02`, `BOOK-03`,
`NEWS-01`, `NEWS-02`, `NEWS-03`, `NEWS-05`,
`BOOKS-01`, `BOOKS-02`, `BOOKS-06`,
`MEET-SEARCH-01`, `MEET-SEARCH-03`, `MEET-SEARCH-04`, `MEET-SEARCH-06`,
`MYPAGE-02`, `MYPAGE-03`, `MYPAGE-04`,
`MEET-HOME-01`,
`MEET-NOTICE-02`, `MEET-NOTICE-03`, `MEET-NOTICE-04`,
`MEET-HOME-04`,
`MEET-BOOKSHELF-02`, `MEET-BOOKSHELF-03`, `MEET-BOOKSHELF-04`, `MEET-BOOKSHELF-05`,
`AUTH-03`,
`MEET-MGMT-01`, `MEET-MGMT-02`, `MEET-MGMT-03`,
`CHAT-01`,
`REPORT-02`, `REPORT-03`

### RN 미완료 (0건)
- 없음 (2026-04-28 기준)

### 공동 추적 (RN 선반영 완료, BE 협의 필요)
| 항목 | 현재 상태 | 비고 |
|------|------|------|
| `MEM-04` | RN fallback 정리 완료 (POST only) | BE 정책/문서 최종 확정 필요 |
| `MEM-09` | RN 준비중 안내 UX 정리 완료 | 차단 API 계약/연동 필요 |
| `AUTH-01` | RN 보상/재시도 흐름 보완 완료 | 서버 원자성(트랜잭션/상태모델) 필요 |

### BE/BE문서 미완료 (미처리)
`BOOK-04`, `BOOK-05`, `BOOKS-03`, `BOOKS-04`, `BOOKS-05`,
`NEWS-04`, `MEET-SEARCH-02`, `MEET-SEARCH-05`,
`MYPAGE-01`, `MEET-HOME-02`, `MEET-HOME-03`,
`MEET-NOTICE-01`, `MEET-BOOKSHELF-01`,
`AUTH-02`, `MEET-MGMT-04`, `MEET-MGMT-05`,
`CHAT-02`, `CHAT-03`, `REPORT-01`,
`MEM-07`

## 정리 메모

- 이번 문서는 "원인 분류 + 변경 방향"만 담았고, 상세 코드 변경안/패치는 별도 작업으로 분리.
