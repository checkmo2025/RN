# ponytail-{관련-기능}-{YYYY-MM-DD}-{HH:mm}

> 이 안내 문장은 복사 후 삭제한다. 파일명은 `ponytail-관련-기능-YYYY-MM-DD-HHmm.md`로 작성한다.

## 1. 작업 요약

| 항목 | 내용 |
| --- | --- |
| 작업 일시 | YYYY-MM-DD HH:mm KST |
| 관련 커밋·PR | 링크 또는 `측정 보류` |
| 대상 기능 | 사용자 관점의 기능 |
| 대상 파일 | 실제 파일 링크 |
| 핵심 구현 변경량 | 추가 N줄 + 삭제 N줄 = N줄 |
| 200줄 예산 사용률 | N% |
| 동작 변경 허용 여부 | 허용하지 않음 / 명시적으로 허용된 내용 |
| 최종 판정 | 확인 / 부분 확인 / 기각 / 측정 보류 |

### 범위와 사용자 동작 계약

변경 후에도 반드시 유지해야 할 동작을 구체적으로 적는다.

- 입력:
- 출력·화면:
- 데이터 ID·개수·순서:
- API 경로·호출 조건:
- 로딩·빈 결과·오류·권한 처리:

## 2. 변경 전

현재 문제와 변경 지점을 설명한다. 코드가 길면 핵심 부분만 남긴다.

```ts
// 핵심 변경 전 코드
```

### 변경 전 기준값

| 지표 | 값 | 측정 방법 |
| --- | ---: | --- |
| 중복 구현 수 |  | `rg`, 코드 검색 |
| 변경 지점 수 |  | 동일 규칙을 수정해야 하는 파일·함수 수 |
| 대상 코드 줄 수 |  | 비어 있지 않은 핵심 코드 기준 |
| API 요청 수 |  | 해당할 때만 기록 |
| 응답시간 중앙값·p95 |  | 해당할 때만 30회 이상 측정 |

## 3. 가설·근거·기대 결과

### 가설

`[변경]`을 하면 `[근거]` 때문에 `[지표]`가 `[기준값]`에서 `[목표값]`으로 바뀌고,
사용자 동작 계약은 그대로 유지될 것이다.

### 근거

- 코드 검색 결과:
- 기존 유틸·플랫폼 기능:
- 병목 또는 중복이 발생하는 이유:

### 기대 결과와 성공 기준

| 지표 | 기준값 | 목표값 | 성공 조건 |
| --- | ---: | ---: | --- |
|  |  |  |  |

성능 개선을 기대하지 않는다면 요청 수·시간복잡도가 유지되는 것을 성공 조건으로 적는다.

## 4. 변경 후

```ts
// 핵심 변경 후 코드
```

### 변경 범위

| 구분 | 추가 | 삭제 | 합계 |
| --- | ---: | ---: | ---: |
| 핵심 구현 코드 |  |  |  |
| 테스트 |  |  |  |
| 문서 |  |  |  |
| 생성물·잠금 파일 |  |  |  |

- 핵심 구현 200줄 이내 여부:
- 200줄 초과 시 예외 사유·분할하지 않은 이유:
- 위험과 복구 방법:

## 5. 측정 방법

변경 전·후에 동일한 조건을 사용한다.

| 항목 | 조건 |
| --- | --- |
| 기기·OS |  |
| 앱 빌드 모드 | development / release |
| 사용자·데이터 |  |
| 네트워크 |  |
| 워밍업·반복 횟수 | 워밍업 5회, 본 측정 30회 이상 |
| 측정 도구·명령 |  |

선택한 지표만 남긴다.

- 구조: 중복 구현 수, 변경 지점 수, 코드 줄 수, 의존성 수
- 동작: 결과 ID·개수·순서, API 경로·커서·호출 횟수, 오류 상태
- 응답: 완료 시간 중앙값·p95
- 렌더링: 렌더 횟수, 긴 프레임·드롭 프레임
- 자원: 메모리, 번들 크기
- 품질: 테스트, TypeScript, ESLint, E2E·실기기

## 6. 측정 결과

| 지표 | 변경 전 | 변경 후 | 변화 | 목표 달성 |
| --- | ---: | ---: | ---: | --- |
|  |  |  |  | 예 / 아니요 / N/A |

### 검증 결과

```sh
# 실제 실행한 명령
```

- 회귀 테스트:
- TypeScript:
- ESLint:
- E2E·시뮬레이터·실기기:
- 측정하지 못한 항목과 이유:

## 7. 가설 판단과 결론

### 판정

`확인 / 부분 확인 / 기각 / 측정 보류`

### 근거

- 가설과 일치한 결과:
- 가설과 달랐던 결과:
- 사용자 동작 계약 보존 여부:

### 결론

포트폴리오에서 설명할 수 있도록 문제, 선택, 수치와 결과를 3~5문장으로 정리한다.

### 후속 작업

측정으로 필요성이 확인된 작업만 적는다. 없으면 `없음`으로 종료한다.
