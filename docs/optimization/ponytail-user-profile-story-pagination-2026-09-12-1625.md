# ponytail-사용자-프로필-책이야기-페이지-수집-공통화-2026-09-12-16:25

## 1. 작업 요약

| 항목 | 내용 |
| --- | --- |
| 작업 일시 | 2026-09-12 16:25 KST |
| 관련 커밋·PR | 비교 기준 `f0d9b77`, 구현은 이 문서의 커밋 |
| 대상 기능 | 다른 사용자 프로필의 책 이야기 전체 페이지 수집 |
| 대상 파일 | [`UserProfileScreen.tsx`](../../src/screens/UserProfileScreen.tsx), 기존 [`pagination.ts`](../../src/utils/pagination.ts) 재사용 |
| 핵심 구현 변경량 | 추가 5줄 + 삭제 19줄 = 24줄 |
| 200줄 예산 사용률 | 12.0% |
| 동작 변경 허용 여부 | 허용하지 않음 |
| 최종 판정 | 확인 — 중복 제거·동작 동등성 확인, 응답속도 개선 주장은 없음 |

### 범위와 사용자 동작 계약

- 입력: 기존 프로필 진입·새로고침·구독 후 프로필 재조회에서 같은 `memberNickname`을 사용한다.
- 출력·화면: `mapRemoteStoryToCard()`를 거친 책 이야기를 기존 화면에 표시한다.
- 데이터 ID·개수·순서: 첫 등장 ID만 보존하고 중복을 제거하되 원래 순서를 유지한다.
- API 경로·호출 조건: 프로필을 먼저 조회·반영하고 책 이야기 커서를 순서대로 조회한다. 첫 커서는 `undefined`, 최대 100페이지다.
- 종료: 다음 페이지 없음, 숫자가 아닌 커서, 이미 본 커서에서 기존처럼 중단한다.
- 로딩·빈 결과·오류·권한: 프로필 조회 오류는 바깥으로 전달한다. 책 이야기 오류는 목록을 비우고 기존 권한별 문구·토스트를 표시한다. 호출부 로딩·새로고침 상태는 수정하지 않는다.
- 공개 서재, 책 이야기 렌더링, 모임, 구독·신고·차단은 수정하지 않는다.

## 2. 변경 전

프로필의 `loadProfile()`에서 공통 `collectAllCursorPages()`와 같은 수집 규칙을 직접 구현한다.

```ts
let cursorId: number | undefined;
const visitedCursors = new Set<number>();
const seenStoryIds = new Set<number>();
const allStories: RemoteStoryItem[] = [];
for (let page = 0; page < 100; page += 1) {
  const response = await fetchMemberBookStories(memberNickname, cursorId);
  response.items.forEach((item) => {
    if (seenStoryIds.has(item.id)) return;
    seenStoryIds.add(item.id);
    allStories.push(item);
  });
  if (!response.hasNext || typeof response.nextCursor !== 'number') break;
  if (visitedCursors.has(response.nextCursor)) break;
  visitedCursors.add(response.nextCursor);
  cursorId = response.nextCursor;
}
```

### 변경 전 기준값

| 지표 | 값 | 측정 방법 |
| --- | ---: | --- |
| 대상 화면의 직접 수집 루프 | 1곳 | `loadProfile()` 확인 |
| 수집 코드의 비어 있지 않은 줄 | 16줄 | 상태 반영 전까지의 수집 영역만 집계 |
| 동일 규칙 구현 지점 | 2곳 | 대상 화면 + 기존 공통 유틸 기준 |
| 최대 페이지 요청 | 100회 | 반복 상한 확인 |
| 의존성 추가 | 0개 | 기존 유틸 사용 가능 |

## 3. 가설·근거·기대 결과

### 가설

직접 구현한 루프를 기존 `collectAllCursorPages()` 호출로 바꾸면 결과와 요청 순서는 같고,
동일 수집 규칙의 수정 지점은 화면·유틸 두 곳에서 유틸 한 곳으로 줄어든다.

### 근거

- 두 구현은 중복 제거 → 다음 페이지·커서 확인 → 방문 커서 기록 순서가 같다.
- 공통 유틸의 기본 상한은 이미 100페이지이며 오류를 삼키지 않으므로 화면의 기존 catch를 재사용할 수 있다.
- 네트워크 요청 수와 순서를 그대로 유지하는 변경이므로 API 응답시간 단축을 가정하지 않는다.

### 기대 결과와 성공 기준

| 지표 | 기준값 | 목표값 | 성공 조건 |
| --- | ---: | ---: | --- |
| 직접 수집 루프 | 1곳 | 0곳 | 공통 유틸 호출로 대체 |
| 결과·요청 순서·오류 처리 | 변경 전 | 동일 | 실제 전후 로더에 동일 응답을 주고 비교 |
| 최대 요청 수 | 100회 | 100회 | 끝나지 않는 페이지 응답으로 검증 |
| 성능 개선 | 가정하지 않음 | 요청 수·복잡도 유지 | 개선율을 억지로 만들지 않음 |

## 4. 변경 후

```ts
const allStories = await collectAllCursorPages({
  fetchPage: (cursorId) => fetchMemberBookStories(memberNickname, cursorId),
  dedupeId: (item) => item.id,
});

setStories(allStories.map(mapRemoteStoryToCard));
```

공통 유틸은 수정하지 않았다. 프로필 조회·상태 반영과 책 이야기 매핑, 기존 catch는 제자리에 두었다.
화면의 수집 영역만 기존 유틸 호출로 대체했다.

### 변경 범위

| 구분 | 추가 | 삭제 | 합계 |
| --- | ---: | ---: | ---: |
| 핵심 구현 | 5줄 | 19줄 | 24줄 |
| 비교·회귀 검사 | 167줄 | 0줄 | 167줄 |
| 문서 | 이 기록, README 1행, CPA 로그 | 0줄 | 구현 예산에서 제외 |
| 실험 결과 | JSON 1개 | 0개 | 115개 비교 사례 |
| 제품 생성물·잠금 파일 | 0줄 | 0줄 | 0줄 |

- 핵심 구현 200줄 이내: 예, 12.0% 사용. 예외 없음.
- 새 의존성: 없음.
- 위험과 복구: 공통 유틸의 수집 규칙이 바뀌면 이 화면에도 적용된다. 최대 페이지·중복·커서·오류 계약은 비교 검사로 보호한다. 복구가 필요하면 이 화면의 수집 루프만 이전 구현으로 되돌린다.

## 5. 측정 방법

실제 변경 전·후 `loadProfile()`과 매핑·오류 함수, 실제 공통 유틸을 실행해
동일한 응답에 대한 API 호출·상태 반영·오류·토스트 기록을 비교한다.
정상·빈 목록·중복·반복 커서·잘못된 커서·100페이지·오류 사례와 결정적 난수 사례를 포함한다.

| 항목 | 조건 |
| --- | --- |
| 실행 일시 | 2026-09-12 16:28:37 KST |
| 실행 환경 | 로컬 Node, 기존 TypeScript, Node 내장 테스트 도구 |
| 기준 코드 | `f0d9b77`의 실제 화면 로더와 변경 후 작업 파일 |
| API·사용자 | 합성 회원·응답; 실제 서버 호출 없음 |
| 호출 기록 | 프로필 요청·상태, 책 이야기 닉네임·커서 요청, 최종 카드, 토스트, 외부로 전달된 오류 |
| 반복·사례 | 명시적 경계 사례 15개 + 고정 seed `20260912`로 만든 페이지열 100개 |
| 테스트 코드 | [`user-profile-pagination.test.mjs`](../../scripts/user-profile-pagination.test.mjs) |
| 원본 결과 | [`115개 사례의 요청 수·커서·ID·문구`](./data/user-profile-story-pagination-2026-09-12.json) |

비교 코드에는 실제 오류 클래스와 판별 함수, 이미지 정규화 함수도 로드한다. 네이티브 상태 훅·API는
기록 함수로 대체한다. 비교는 API부터 오류까지 전체 기록을 대상으로 하며 JSON에는 주요 결과를
보관한다. 시간 측정용 실험이 아니므로 테스트 실행에 걸린 시간을 앱 응답시간으로 해석하지 않는다.
비교 기준 커밋을 읽으므로 재현 시 저장소에 `f0d9b77` 이력이 있어야 한다.

## 6. 측정 결과

| 지표 | 변경 전 | 변경 후 | 변화 | 목표 달성 |
| --- | ---: | ---: | ---: | --- |
| 화면의 직접 수집 루프 | 1곳 | 0곳 | 1곳 제거 | 예 |
| 수집 코드의 비어 있지 않은 줄 | 16줄 | 4줄 | 75.0% 감소 | 예 |
| 동일 규칙 구현 지점 | 2곳 | 1곳 | 대상 화면+공통 유틸 범위 | 예 |
| 115개 사례의 책 이야기 요청 합계 | 518회 | 518회 | 0회 변화 | 예 |
| API·상태·결과·오류 기록 불일치 | 기준 | 0건 | 115/115 동일 | 예 |
| 최대 페이지 수 | 100회 | 100회 | 동일 | 예 |
| 직접 의존성 추가 | 0개 | 0개 | 동일 | 예 |
| 시간복잡도 | O(N+P) | O(N+P) | 구조상 동일 | 예 |

N은 받은 항목 수, P는 요청 페이지 수다. 시간복잡도는 코드에서 확인한 구조 지표다.
프로필 요청도 모든 사례에서 1회씩, 전후 각 115회로 같았다.

### 비교 실험 상세

| 사례 | 사례 수 | 책 이야기 요청 전→후 | 결과 |
| --- | ---: | ---: | --- |
| 정상·중복·순서·매핑 | 1 | 2→2 | 첫 등장 ID·제목·표지 정규화 보존 |
| 빈 목록 | 1 | 1→1 | 빈 카드 배열 보존 |
| 반복·잘못된 커서·숫자 0 | 5 | 7→7 | 중단 조건·0 커서 처리 보존 |
| 끝나지 않는 다음 페이지 | 1 | 100→100 | 101번째 요청 없음 |
| 후속 페이지 일반·401·403·404·500 오류 | 6 | 12→12 | 부분 결과 제거·기존 문구 보존 |
| 프로필 조회 오류 | 1 | 0→0 | 책 이야기 미조회·오류 전달 보존 |
| 고정 seed 페이지열 | 100 | 396→396 | 전체 기록 일치 |
| 합계 | 115 | 518→518 | 불일치 0건 |

### 검증 결과

```sh
CHECKMO_PAGINATION_REPORT=docs/optimization/data/user-profile-story-pagination-2026-09-12.json \
  node --test scripts/user-profile-pagination.test.mjs
node --test scripts/user-profile-library.test.mjs
npm run typecheck
npx eslint src/screens/UserProfileScreen.tsx --max-warnings 200
npm run check:typography
npm run check:spacing
npx expo export --platform ios --output-dir /private/tmp/checkmo-profile-pagination-export-20260912
git diff --check
```

- 페이지 수집 검사 8개, 공개 서재·책 이야기 렌더 회귀 검사 6개 통과.
- TypeScript·디자인 토큰·공백 검사 통과.
- ESLint 오류 0개, 기존 미사용 변수 경고 2개 유지.
- 제품 진입점 iOS 프로덕션 Hermes export 통과.
- 데이터 매핑·공개 서재·모임·화면 렌더·기존 오류 처리에는 diff 없음.
- 실서버 응답시간·네이티브 프레임·메모리는 이 변경의 측정 대상이 아니다. 합성 응답을 쓴 동일 동작 비교이므로 실제 계정의 E2E 검증으로 주장하지 않는다.

## 7. 가설 판단과 결론

### 판정

`확인`

### 근거

- 대상 화면의 중복 수집 루프가 없어지고 기존 공통 유틸만 호출한다.
- 115개 비교 사례의 결과·API 호출·상태·오류 기록이 모두 같았다.
- 책 이야기 요청 합계가 518회로 유지되어 네트워크 절감이나 응답시간 개선을 주장할 근거는 없다.

### 결론

사용자 프로필의 책 이야기 전체 수집을 기존 `collectAllCursorPages()`로 통합했다.
수집 코드는 16줄에서 4줄로 75.0% 줄고, 대상 화면과 공통 유틸에 나뉘었던 동일 규칙이 한 곳으로 모였다.
실제 전후 로더에 같은 응답을 제공한 115개 사례에서 불일치는 0건이고 요청 수는 각각 518회로 같았다.
이번 변경은 응답속도 향상이 아니라 기능을 보존하면서 유지보수 지점을 줄인 결과다.

### 후속 작업

없음. 이번 실험으로 추가 캐시·병렬 조회·페이지 제한 변경을 도입할 근거는 없다.
