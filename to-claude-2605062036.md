# to-claude-2605062036

작성 시각: 2026-05-06 20:40:50 KST
브랜치: `main`

## 목표
`MeetingScreen` 분해 작업을 현재 중간 상태에서 바로 이어서, `MEET-SPLIT-04`/`MEET-SPLIT-05`를 완료할 수 있도록 컨텍스트를 전달한다.

## 이번 단계에서 이미 끝난 것
- 분리 파일 생성 완료
  - `src/screens/meeting/types.ts`
  - `src/screens/meeting/helpers.ts`
  - `src/screens/meeting/useNoticeState.ts`
  - `src/screens/meeting/useBookshelfState.ts`
  - `src/screens/meeting/useManagementState.ts`
  - `src/screens/meeting/GroupNoticeView.tsx`
  - `src/screens/meeting/GroupBookshelfView.tsx`
  - `src/screens/meeting/meetingStyles.ts`
- 아키텍처/표준화 문서 생성 완료 (`docs/documents/app-architecture-*`, `app-standardization-*`)
- 타입체크 통과
  - `npm run typecheck` (`tsc --noEmit`) 성공

## 방금 수정된 타입 에러 포인트 (되돌리지 말 것)
- `useNoticeState.ts`
  - `../../utils/date`에서 `toApiDateTimeFromFormatted` import 제거 (`존재하지 않는 export`)
- `useBookshelfState.ts`
  - `getCurrentKstDateLabel` import를 `./formatters` -> `../../utils/date`로 변경

## 현재 구조 상태
- `src/screens/MeetingScreen.tsx`가 여전히 거대 컨테이너 상태 (`~10k lines`)
- `GroupHomeView` 내부에서 로컬 state/effect/handler를 대부분 계속 보유 중
- 분리된 hook/view 파일은 생성만 되어 있고, 실제 조립 연결은 미완료

## 바로 해야 할 남은 작업 (우선순위 순)
1. `GroupManagementOverlay` 생성
- 신규 파일: `src/screens/meeting/GroupManagementOverlay.tsx`
- 소스 추출 위치: `src/screens/MeetingScreen.tsx`의 관리 오버레이 영역
  - 시작 근처: `visible={managementMenuVisible || Boolean(activeManagementScreen) || bookshelfBookSelectorVisible}`
  - 대략 라인: `7635+`
- `bookshelfBookSelectorVisible` 분기, `activeManagementScreen` 분기, 하단 footer 저장/삭제 버튼 분기까지 컴포넌트로 이동

2. `GroupHomeView`를 신규 hook + view 조립형으로 변경
- 대상: `src/screens/MeetingScreen.tsx` 내 `function GroupHomeView` (대략 `1921+`)
- 해야 할 것
  - `useNoticeState`, `useBookshelfState`, `useManagementState` 호출로 상태/핸들러 소유권 이전
  - 로컬 중복 state/effect/handler 제거
  - 필요한 setState 의존성만 상위/하위로 연결

3. 공지/책장 탭 JSX 교체
- 공지 탭 블록 (대략 `6220~6618`) -> `GroupNoticeView` 사용
- 책장 탭 블록 (대략 `6620+`) -> `GroupBookshelfView` 사용
- props는 각 컴포넌트의 `*Props` 타입 정의를 기준으로 정리

4. `MeetingScreen` 타입/도우미 소스 정리
- `./meeting/types`, `./meeting/helpers`를 실제로 사용하도록 타입/함수 중복 제거
- `MeetingScreen.tsx` 내 미사용 import/상수 정리

5. 검증
- `npm run typecheck`
- 가능하면 최소 스모크: `npm run check`

## TODO 상태 기준
- `docs/agent/todo.md`
  - `[MEET-SPLIT-03]` = `🔄`
  - `[MEET-SPLIT-04]` = `🔄`
  - `[MEET-SPLIT-05]` = `⬜`
- 위 3개는 남은 작업 완료 후 상태 갱신 필요

## 주의
- 워킹트리에 작업과 무관한 untracked 디렉터리 존재:
  - `.codex/`, `checkmo_fe2/`, `checkmo_be 2/`, `skills/`
- 커밋 시 반드시 관련 파일만 스테이징할 것.
