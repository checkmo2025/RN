# to-claude-2605072230

작성 시각: 2026-05-07 22:35:00 KST
브랜치: `main`

## 목표
`MEET-SPLIT-05` (MeetingScreen 컨테이너 축소/조립 전용화)를 완료한다.

## 이전 세션에서 완료된 것
- `MEET-SPLIT-01` ✅ 설계/경계 정의
- `MEET-SPLIT-02` ✅ formatter/mapper 순수 함수 분리
- `MEET-SPLIT-03` ✅ GroupHomeView hook 조립 완료 (8099→4475줄, -3624줄)
  - `useNoticeState`, `useBookshelfState`, `useManagementState` 3개 hook 조립
  - proxy ref 패턴으로 circular dependency 해결
  - `tsc --noEmit` 통과
  - 커밋: `e717bc7`
- `MEET-SPLIT-04` ✅ 하위 View 컴포넌트 분리 (GroupNoticeView, GroupBookshelfView, GroupManagementOverlay)

## 현재 구조 상태
- `src/screens/MeetingScreen.tsx`: **4475줄**
- 분리 완료 파일들:
  - `src/screens/meeting/types.ts`
  - `src/screens/meeting/helpers.ts`
  - `src/screens/meeting/formatters.ts`
  - `src/screens/meeting/mappers.ts`
  - `src/screens/meeting/useNoticeState.ts`
  - `src/screens/meeting/useBookshelfState.ts`
  - `src/screens/meeting/useManagementState.ts`
  - `src/screens/meeting/GroupNoticeView.tsx`
  - `src/screens/meeting/GroupBookshelfView.tsx`
  - `src/screens/meeting/GroupManagementOverlay.tsx`
  - `src/screens/meeting/meetingStyles.ts`

## MEET-SPLIT-05에서 해야 할 것

### 목표
`MeetingScreen.tsx`의 역할을 라우팅/탭/상태 조립 중심으로 축소.
완료 기준(DoD): 파일 길이와 내부 state/effect 수가 초기(10091줄) 대비 의미 있게 감소하고 `tsc --noEmit` 통과.

### 구체 작업 목록

1. **MeetingScreen.tsx 상단부 정리** (`~200줄`)
   - `./meeting/types`, `./meeting/helpers`, `./meeting/formatters`, `./meeting/mappers`를 실제로 사용하도록 중복 타입/함수 제거
   - 미사용 import 정리

2. **GroupSearchView 분리 검토** (대략 `1~1920줄` 내 search 관련 블록)
   - `GroupSearchView` 함수가 아직 MeetingScreen.tsx 내부에 있다면 별도 파일로 추출

3. **MeetingScreen 루트 함수 내 남은 로컬 state 점검**
   - `MeetingScreen` 루트 함수(GroupHomeView 바깥)에 아직 남은 state/effect가 있는지 확인
   - hook으로 이미 이전된 것들의 잔재 제거

4. **`./meeting/types`와 `./meeting/helpers` 실제 사용 연결**
   - MeetingScreen.tsx 내 중복 타입 정의/함수가 있다면 import로 교체

5. **검증**
   - `npm run typecheck`
   - (가능하면) `npm run check`

## TODO 상태
- `docs/agent/todo.md`
  - `[MEET-SPLIT-03]` = `✅` (방금 완료)
  - `[MEET-SPLIT-04]` = `✅`
  - `[MEET-SPLIT-05]` = `⬜` → 이번 세션에서 `✅`로 변경 필요

## 주의
- 워킹트리 무관 untracked: `.codex/`, `checkmo_fe2/`, `checkmo_be 2/`, `skills/`
- 커밋 시 관련 파일만 스테이징
- `agent-log.md` 변경은 같은 커밋에 포함
- 커밋/푸시는 `/cpa` 워크플로우 따를 것 (todo → agent-log → 검증 → 스테이징 → 커밋 → 푸시)
