# Agent Instructions

- If a user request ends with `/md`, append a short update to `codex.md`.
- The appended update must include:
  - Date (`YYYY-MM-DD`)
  - Time (`HH:mm:ss KST`)
  - Brief summary of what was changed
- Keep the summary concise (1-4 bullet points).
