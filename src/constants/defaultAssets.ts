import { Image } from 'react-native';

export const CLUB_DEFAULT_IMAGE = Image.resolveAssetSource(
  require('../../assets/images/club-default.png'),
).uri;

export const NEWS_DEFAULT_IMAGE = Image.resolveAssetSource(
  require('../../assets/images/news-default.png'),
).uri;

export const BOOK_DEFAULT_IMAGE = Image.resolveAssetSource(
  require('../../assets/images/book-default.png'),
).uri;
